// The config width for column in table
const WIDTH_COLUMN_CONFIG = {
  SMALL: 80,
  MEDIUM: 100,
  LARGE: 150,
};

// Default size of table columns
const SIZE_COLUMN_DEFAULT = 120;

export { WIDTH_COLUMN_CONFIG, SIZE_COLUMN_DEFAULT };
